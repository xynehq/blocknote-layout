# blocknote-layout-server-utils

React-free server-side block specs for all blocknote-layout blocks (whiteboard, genius, coderunner, mentions), for use with `@blocknote/server-util`. Zero UI dependencies.

```bash
pnpm add blocknote-layout-server-utils
```

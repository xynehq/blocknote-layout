# blocknote-layout-extensions

Whiteboard, slideshow, and mentions extensions for BlockNote, bundled in one package. For the full set (including code runner and genius) use `blocknote-layout`; for single blocks use the individual `blocknote-layout-<block>` packages.

```bash
pnpm add blocknote-layout-extensions
```
